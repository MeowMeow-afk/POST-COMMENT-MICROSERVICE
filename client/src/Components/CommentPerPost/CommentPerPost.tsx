import React, { useEffect } from 'react';

const CommentPerPost = () => {
  useEffect(() => {
    // return () => {};
  }, []);

  return <div>CommentPerPost</div>;
};

export default CommentPerPost;
